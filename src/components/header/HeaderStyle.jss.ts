import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  genderText: {
    fontWeight: 600,
    fontSize: '21px',
    lineHeight: '31px',
    color: '#000000'
  }
});
